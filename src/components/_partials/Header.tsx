import React from "react";
import styled from "styled-components";

const Container = styled.header`
  font-size: 48px;
  font-weight: 500;
  margin-bottom: 2em;
`;

const Header = () => {
  return <Container>Acoustic article page</Container>;
};

export default Header;
